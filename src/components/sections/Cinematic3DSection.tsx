import { CinematicScene } from "@/components/animations/CinematicScene";

export const Cinematic3DSection = () => {
  return (
    <section className="relative w-full overflow-hidden bg-background">
      <CinematicScene />
    </section>
  );
};
